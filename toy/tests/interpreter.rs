use toy::instruction::{ArithOp, Instruction};
use toy::interpreter::{Interpreter, eval};
use toy::val::Val;

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn interpreter_1() {
        let I = Interpreter::new();
    }
}
