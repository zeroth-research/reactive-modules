[package]
name = "theory"
version = "0.1.0"
edition = "2024"

[dependencies]
tch = { version = "0.22", optional = true }
pyo3 = { version = "0.24" }
pyo3-tch = { version = "0.22" }

[features]
torch = ["dep:tch"]
